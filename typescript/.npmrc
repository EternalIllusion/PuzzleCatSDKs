# PuzzleCat SDK 发布配置
# 锁定 npm 官方 registry：本机若配置了镜像源（如 npmmirror），
# 未显式指定时 npm/pnpm publish 可能发布到镜像源而失败或发错位置
registry=https://registry.npmjs.org/

# 注意：发布凭据（token）不要写在本文件
# 本地发布请执行 npm login（凭据存于用户级 ~/.npmrc），CI 发布使用 NODE_AUTH_TOKEN 环境变量
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}